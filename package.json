{
  "name": "movie-time",
  "version": "1.2.0",
  "description": "Watch Netflix together with real-time sync and built-in video chat (P2P)",
  "scripts": {
    "build:popup": "vite build --mode popup",
    "build:content": "vite build --mode content",
    "build:background": "vite build --mode background",
    "build:bridge": "vite build --mode bridge",
    "build": "npm run build:popup && npm run build:content && npm run build:background && npm run build:bridge && cp manifest.json dist/ && cp -r icons dist/"
  },
  "dependencies": {
    "webrtc-adapter": "^8.0.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.2.0",
    "@types/chrome": "^0.1.22",
    "@types/node": "^24.7.0",
    "tslib": "^2.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.9"
  },
  "private": true
}
