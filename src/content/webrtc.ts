export function setupWebRTC() {
  // TODO: implementa creazione di RTCPeerConnection,
  // scambio offerta/risposta (signaling),
  // getUserMedia, aggiunta stream remoto a overlay video DOM.
  console.log("setupWebRTC (stub)");
}
